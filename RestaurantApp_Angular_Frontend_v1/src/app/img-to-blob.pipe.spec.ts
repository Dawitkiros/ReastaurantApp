import { ImgToBlobPipe } from './img-to-blob.pipe';

describe('ImgToBlobPipe', () => {
  it('create an instance', () => {
    const pipe = new ImgToBlobPipe();
    expect(pipe).toBeTruthy();
  });
});
